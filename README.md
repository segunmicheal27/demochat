# demochat

WebSocket server for SwissPay Chat application.

## Hosting on Railway/Render

1. Connect this repository to your hosting provider.
2. The server uses the `PORT` environment variable or defaults to `8080`.
3. Use `wss://your-domain.up.railway.app` in the Flutter app.
